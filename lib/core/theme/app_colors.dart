import 'package:flutter/material.dart';

// ── Thème : noir / gris / blanc (style Angular BadWallet) ───────────────────
const kBg      = Color(0xFF0D0D0D);   // fond principal — noir profond
const kSurface = Color(0xFF171717);   // surfaces secondaires
const kCard    = Color(0xFF1F1F1F);   // cartes / panneaux
const kBorder  = Color(0xFF2E2E2E);   // bordures subtiles

// ── Seules couleurs vives : imposées par le prof ────────────────────────────
const kPrimary = Color(0xFFFFFFFF);   // accent principal — blanc
const kGreen   = Color(0xFF22C55E);   // vert  — crédits / dépôts reçus
const kRed     = Color(0xFFEF4444);   // rouge — débits / transferts envoyés
const kOrange  = Color(0xFFD4D4D4);   // gris clair — factures
const kBlue    = Color(0xFFD4D4D4);   // gris clair — info

// ── Typographie ─────────────────────────────────────────────────────────────
const kT1 = Color(0xFFFFFFFF);   // texte principal — blanc pur
const kT2 = Color(0xFFA3A3A3);   // texte secondaire — gris moyen
const kT3 = Color(0xFF525252);   // texte désactivé — gris foncé

// ── Variantes translucides ──────────────────────────────────────────────────
const kPrimaryFaint = Color(0x1AFFFFFF);
const kGreenFaint   = Color(0x1A22C55E);
const kRedFaint     = Color(0x1AEF4444);
const kOrangeFaint  = Color(0x1AD4D4D4);
const kBlueFaint    = Color(0x1AD4D4D4);
